# 📋 VAD (Voice Activity Detection) Детальное логирование

## Описание

Task 2.3 добавляет детальное логирование системы VAD на трех уровнях для улучшения отладки и верификации корректной работы.

## Уровни логирования

### DEBUG уровень (детальная информация для разработчиков)

Логирует на каждом цикле мониторинга детальную информацию о состоянии VAD:

```
[DEBUG] VAD monitoring: recording_duration=2.34s, silence_duration=0.12s, activity_updates_count=8
```

- `recording_duration` - как долго идет текущая запись
- `silence_duration` - длительность молчания без активности
- `activity_updates_count` - сколько раз была обновлена активность

Анализ частоты активности (для определения режима):

```
[DEBUG] VAD frequency analysis: avg_interval=0.28s, adaptive_timeout=3.50s, expected_mode=continuous
```

- `avg_interval` - средний интервал между активностями
- `adaptive_timeout` - выбранное значение timeout для этого режима
- `expected_mode` - определенный режим (continuous для быстрых обновлений, intermittent для редких)

Обнаружение непрерывной речи:

```
[DEBUG] VAD: Continuous speech detected (interval=0.28s)
```

Финальная статистика:

```
[DEBUG] VAD final stats: avg_interval=0.32s, mode=continuous
```

### INFO уровень (важные события и режимные переходы)

Обнаружение тишины:

```
[INFO] VAD: Тишина обнаружена для channel-123 (длительность: 0.50s, timeout=2.0s, threshold=2.00s)
```

Окончание речи:

```
[INFO] VAD: Окончание речи детектировано для channel-123 (silence_duration=2.15s, recording_duration=8.50s, activity_updates=45)
```

Максимальное время записи достигнуто (fallback):

```
[INFO] VAD: Максимальное время записи достигнуто для channel-123 (12.50s >= 15.0s), updates_count=32
```

Переключение в режим grace period:

```
[DEBUG] VAD: Switching to grace period mode (duration=5.0s), silence_timeout_override=2.0s
```

Завершение записи:

```
[INFO] ✅ VAD запись завершена для channel-123: reason=silence_detected, recording_duration=8.50s, recording_id=rec-456
```

### WARNING уровень (потенциальные проблемы)

Необычно длинная запись:

```
[WARNING] VAD: Unusually long recording (35.20s), check if grace period is working, activity_updates=12
```

Очень мало обновлений активности:

```
[WARNING] VAD: Very few activity updates (1) for long recording (7.30s)
```

## Трассировка полного пути выполнения

Логирование позволяет проследить, почему был выбран тот или иной timeout:

1. **DEBUG**: Видим текущие метрики мониторинга
2. **DEBUG**: Видим анализ частоты активности и определенный режим
3. **DEBUG/INFO**: Видим обнаружение тишины и ее характеристики
4. **INFO**: Видим финальное решение о завершении записи
5. **WARNING**: Видим потенциальные проблемы

### Пример полного потока логов для одной записи:

```
[INFO] ✅ VAD мониторинг запущен для канала channel-123 (silence_timeout=2.0s)
[DEBUG] VAD: Активность обновлена для канала channel-123 (recording_duration=0.15s, time_since_previous_activity=0.10s)
[DEBUG] VAD monitoring: recording_duration=0.20s, silence_duration=0.05s, activity_updates_count=1
[DEBUG] VAD: Активность обновлена для канала channel-123 (recording_duration=0.45s, time_since_previous_activity=0.30s)
[DEBUG] VAD monitoring: recording_duration=0.50s, silence_duration=0.05s, activity_updates_count=2
[DEBUG] VAD frequency analysis: avg_interval=0.30s, adaptive_timeout=2.0s, expected_mode=continuous
[DEBUG] VAD: Continuous speech detected (interval=0.30s)
[INFO] VAD: Тишина обнаружена для channel-123 (длительность: 2.10s, timeout=2.0s, threshold=2.00s)
[INFO] VAD: Окончание речи детектировано для channel-123 (silence_duration=2.15s, recording_duration=8.50s, activity_updates=45)
[DEBUG] VAD final stats: avg_interval=0.32s, mode=continuous
[INFO] ✅ VAD запись завершена для channel-123: reason=silence_detected, recording_duration=8.50s, recording_id=rec-456
```

## Поиск логов в файлах логов

### Все VAD события:
```bash
grep "VAD" /var/log/metrotech/bot.log
```

### Только ошибки VAD:
```bash
grep "❌.*VAD" /var/log/metrotech/bot.log
```

### Только окончания записи:
```bash
grep "Окончание речи детектировано" /var/log/metrotech/bot.log
```

### Только проблемы (WARNING):
```bash
grep "\[WARNING\].*VAD" /var/log/metrotech/bot.log
```

### Для конкретного канала:
```bash
grep "channel-123" /var/log/metrotech/bot.log | grep VAD
```

## Включение DEBUG логирования

Для более детального логирования установите переменную окружения:

```bash
export VAD_DEBUG_LOGGING=true
```

или в `.env`:

```
VAD_DEBUG_LOGGING=true
```

## Метрики для анализа

При анализе логов обратите внимание на следующие метрики:

### `avg_interval` - средний интервал между активностями
- **< 0.5s**: Очень быстрые обновления, может быть шум
- **0.5-2.0s**: Нормальная непрерывная речь (continuous mode)
- **> 2.0s**: Редкие обновления, прерывистая речь (intermittent mode)

### `activity_updates_count` - количество обновлений активности
- **< 2 для записи > 5s**: Может быть проблема с обнаружением активности
- **32+**: Нормальное количество для средней записи
- **> 100**: Может быть очень активная или шумная речь

### `recording_duration` - общая длительность записи
- **< 2s**: Очень короткая запись, возможно пользователь не говорил
- **2-10s**: Нормальная запись для одного предложения
- **> 30s**: Необычно длинная, проверьте grace period

### `silence_duration` - длительность молчания
- **0.5-2.0s**: Нормальная пауза между словами
- **> 2.0s**: Обозначает конец речи в continuous режиме
- **> 5.0s**: Явно определен конец речи в любом режиме

## Требования к логированию (Acceptance Criteria)

✅ **Логирование на разных уровнях**
- DEBUG: детали для каждого цикла мониторинга VAD
- INFO: важные события (переход в режимы, конец записи)  
- WARNING: потенциальные проблемы

✅ **Форматирование**
- Логи содержат временные метки (есть по умолчанию)
- Логи содержат контекст (какой режим, какие значения)
- Примеры логов легко искать в логах бота

✅ **Трассировка**
- Можно проследить полный путь: почему был выбран тот или иной timeout
- Логи содержат все параметры, влияющие на решение

## Интеграция с мониторингом

Логи VAD содержат достаточно информации для интеграции с системами мониторинга:

### Метрики для мониторинга:
- `vad.recording_duration` - длительность записи
- `vad.silence_duration` - длительность молчания  
- `vad.activity_updates` - количество обновлений активности
- `vad.avg_interval` - средний интервал между активностями
- `vad.adaptive_timeout` - выбранный timeout

### События для оповещений:
- Необычно длинная запись (> 30s) - потенциальная проблема
- Мало обновлений активности для длинной записи - проблема обнаружения
- Ошибки в VAD callback - ошибка обработки

## Дополнительная информация

- **Где логируется**: `/var/log/metrotech/bot.log` (если настроено)
- **Добавлено в**: `app/backend/services/simple_vad_service.py`
- **Методы с логированием**:
  - `update_activity()` - отслеживание активности пользователя
  - `_monitor_silence()` - основной цикл мониторинга с анализом частоты
  - `_finish_recording()` - завершение записи с итоговой статистикой
  - `record_until_silence_with_soft_window()` - режим grace period
