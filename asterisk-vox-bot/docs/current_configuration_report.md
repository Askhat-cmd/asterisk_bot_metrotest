# Текущее состояние конфигурации

_Отчёт подготовлен в рамках Шага 1 миграции настроек (изучение существующей конфигурации)._  
_Дата: 2024-11-11_

## 1. Источники данных

| Файл | Статус чтения | Комментарий |
|------|---------------|-------------|
| `../настройки_из_файла_env.txt` | ✔ | Файл найден, однако он пуст (0 байт). Значения переменных не предоставлены. |
| `./.env` | ✖ | Файл отсутствует в репозитории и в рабочих каталогах `/home/engine`, `~/project`, `~/project/asterisk-vox-bot`. |
| `/etc/asterisk/extensions.conf` | ✖ | Каталог `/etc/asterisk` отсутствует в окружении, файл не найден. |

## 2. Сводка по конфигурационным параметрам

Так как исходный файл с полным набором переменных окружения пуст, фактические значения параметров отсутствуют. Ниже сохранена структура отчёта по ожидаемым категориям с отметкой о наличии данных.

| Категория | Переменная | Значение | Описание | Связь с barge-in |
|-----------|------------|----------|----------|------------------|
| ARI | – | – | Значения не предоставлены в исходном файле. | – |
| Redis | – | – | Значения не предоставлены в исходном файле. | – |
| Yandex (ASR/TTS) | – | – | Значения не предоставлены в исходном файле. | – |
| VAD | – | – | Значения не предоставлены в исходном файле. | – |
| Barge-in | – | – | Значения не предоставлены в исходном файле. | – |
| Прочие сервисы | – | – | Значения не предоставлены в исходном файле. | – |

> **Примечание.** В исходном файле `настройки_из_файла_env.txt` не было ни одной строки, поэтому перечислить реальные значения невозможно. Если появится актуальная копия файла с данными, таблицу можно будет заполнить без изменения структуры.

## 3. Сравнение `настройки_из_файла_env.txt` и `.env`

Файл `.env` в рабочем окружении отсутствует, поэтому сравнение невозможно. 

| Переменная | Значение в `настройки_из_файла_env.txt` | Значение в `.env` | Комментарий |
|------------|-----------------------------------------|-------------------|-------------|
| – | – | – | Одно из двух источников отсутствует/пуст. |

## 4. Настройки Asterisk (`/etc/asterisk/extensions.conf`)

- Каталог `/etc/asterisk` отсутствует, файл `extensions.conf` недоступен.
- Параметр `TALK_DETECT` не найден, задокументировать его текущее значение не представляется возможным.
