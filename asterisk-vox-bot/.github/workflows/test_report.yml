name: Test report in PR

on:
  workflow_run:
    workflows: ["Run tests"]
    types: [completed]

jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - name: Post PR Comment
        uses: peter-evans/create-or-update-comment@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.workflow_run.pull_requests[0].number }}
          body: |
            🧪 **Тесты завершены.**
            Статус: **${{ github.event.workflow_run.conclusion }}**
            Подробности: [${{ github.event.workflow_run.name }}](${{ github.event.workflow_run.html_url }})
