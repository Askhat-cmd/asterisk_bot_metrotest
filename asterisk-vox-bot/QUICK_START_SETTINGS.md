# 🚀 Быстрый старт: Использование централизованных настроек

## ✅ Что готово к использованию

### 1. Базовая инфраструктура настроек создана
- ✅ `app/backend/config/settings.py` - централизованный модуль настроек
- ✅ `.env.example` - шаблон конфигурации с комментариями
- ✅ `test_settings.py` - тестовый скрипт для проверки
- ✅ Автоматическая загрузка в `main.py` при старте приложения

### 2. Настройки валидируются автоматически
- URL'ы проверяются на корректность формата
- Таймауты проверяются на допустимый диапазон значений
- При ошибке валидации приложение не запустится

### 3. Доступные настройки (Step 1)

#### Asterisk ARI:
- `ari_http_url` - URL для подключения к ARI
- `ari_username` - имя пользователя
- `ari_password` - пароль
- `ari_app_name` - имя Stasis приложения

#### Redis:
- `redis_url` - URL для подключения к Redis

#### Таймауты:
- `speech_end_timeout` - таймаут окончания речи (0.2s по умолчанию)
- `max_silence_duration` - максимальная длительность тишины (5.0s)

## 📖 Как использовать

### В коде:

```python
from app.backend.config.settings import settings

# Получение значений
ari_url = settings.ari_http_url
redis_url = settings.redis_url
timeout = settings.speech_end_timeout

# Все настройки имеют type hints для IDE
print(f"Подключаемся к ARI: {settings.ari_http_url}")
```

### Настройка окружения:

1. **Копируем шаблон:**
   ```bash
   cp .env.example .env
   ```

2. **Редактируем .env:**
   ```bash
   nano .env
   # или
   vim .env
   ```

3. **Проверяем настройки:**
   ```bash
   python test_settings.py
   ```

## 🎯 Важно

### ⚠️ На этом шаге:
- **НЕ ЗАМЕНЕНЫ** константы в коде
- **НЕ ИЗМЕНЕНЫ** существующие сервисы  
- Создана только **инфраструктура** для настроек

### ✅ Это значит:
- Приложение работает как раньше
- Старые настройки через `os.getenv()` продолжают работать
- Новые настройки **доступны**, но не используются в коде (пока)

### 🔜 Следующие шаги:
**Шаг 2-4**: Постепенная замена `os.getenv()` на использование `settings` во всех файлах

## 🧪 Тестирование

### Тест настроек:
```bash
python test_settings.py
```

Ожидаемый вывод:
```
✅ Настройки успешно загружены!

📋 ASTERISK ARI:
  • URL:      http://localhost:8088
  • Username: asterisk
  • Password: ***********
  • App Name: asterisk-bot

📋 REDIS:
  • URL: redis://localhost:6379/0

📋 ТАЙМАУТЫ:
  • Speech end timeout:   0.2s
  • Max silence duration: 5.0s

✅ ВСЕ ТЕСТЫ ПРОШЛИ УСПЕШНО
```

### Запуск приложения:
```bash
# В логах появятся строки о загрузке настроек:
INFO - 🔧 Загрузка централизованных настроек...
INFO - ✅ Настройки загружены успешно:
INFO -   - ARI URL: http://localhost:8088
INFO -   - ARI App: asterisk-bot
INFO -   - Redis URL: redis://localhost:6379/0
INFO -   - Speech end timeout: 0.2s
INFO -   - Max silence duration: 5.0s
```

## 📝 Примеры использования

### Пример 1: Простое чтение настроек
```python
from app.backend.config.settings import settings

def connect_to_ari():
    url = settings.ari_http_url
    username = settings.ari_username
    password = settings.ari_password
    app = settings.ari_app_name
    
    print(f"Connecting to {url} as {username}")
    # ... код подключения
```

### Пример 2: Использование в классах
```python
from app.backend.config.settings import settings

class MyService:
    def __init__(self):
        self.timeout = settings.speech_end_timeout
        self.redis_url = settings.redis_url
    
    def process(self):
        print(f"Using timeout: {self.timeout}s")
```

### Пример 3: Перезагрузка настроек (если .env изменился)
```python
from app.backend.config.settings import reload_settings

# После изменения .env файла во время работы приложения
new_settings = reload_settings()
print(f"Новый таймаут: {new_settings.speech_end_timeout}")
```

## 🐛 Решение проблем

### Проблема: ModuleNotFoundError: No module named 'pydantic_settings'
**Решение:**
```bash
pip install pydantic-settings
```

### Проблема: ValidationError при запуске
**Причина:** Некорректное значение в .env файле

**Решение:**
1. Проверьте формат URL'ов (должны начинаться с http:// или redis://)
2. Проверьте диапазон значений для таймаутов
3. Запустите `python test_settings.py` для диагностики

### Проблема: Настройки не загружаются
**Решение:**
1. Убедитесь, что .env файл существует в корне проекта
2. Проверьте, что переменные в .env указаны в UPPER_CASE
3. Убедитесь, что python-dotenv установлен

## 📚 Полная документация

Смотрите `SETTINGS_MIGRATION_STEP1.md` для подробной технической документации.

---

**Статус**: ✅ Готово к использованию  
**Версия**: Step 1 of 4
