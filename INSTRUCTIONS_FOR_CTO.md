# 🎯 ИНСТРУКЦИЯ ДЛЯ CTO.NEW АГЕНТА

## 📌 Цель задачи

Исправить критические проблемы в голосовом боте на Asterisk, связанные с:
1. **Underrun** (обрыв аудио при воспроизведении)
2. **Преждевременное прерывание записи пользователя** (VAD детектирует тишину, хотя пользователь продолжает говорить)
3. **Оптимизация Voice Activity Detection (VAD)** для стабильной работы

---

## 📂 Вспомогательные файлы в репозитории

### 1. **`BARGE_IN_PROBLEM_ANALYSIS.md`** ⭐ ОСНОВНОЙ ДОКУМЕНТ
**Путь:** `/BARGE_IN_PROBLEM_ANALYSIS.md`

Содержит:
- Детальное описание проблемы
- История архитектурных решений
- Технический анализ текущей реализации
- 4 предложенных эксперимента (Experiment 1-4)
- Анализ логов с примерами проблем

**Ключевые разделы для изучения:**
- `## 🐛 Текущие проблемы` - описание симптомов
- `## 🔬 Эксперименты` - предложенные решения
- `## 📊 Анализ логов` - конкретные примеры ошибок

### 2. **`Логи_бота.txt`** 📋
**Путь:** `/Логи_бота.txt`

Реальные логи работы бота с проблемами:
- Сообщения `Buffer underrun detected`
- Преждевременные `VAD: Тишина обнаружена`
- События `ChannelTalkingStarted` / `ChannelTalkingFinished`
- Метрики времени обработки

**Что искать в логах:**
```
🎧 [BUFFER UNDERRUN] detected  ← Проблема #1
VAD: Тишина обнаружена         ← Проблема #2 (хотя юзер говорит)
time_since_activity=1.52s       ← Метрики VAD
```

### 3. **`настройки_из_файла_env.txt`** ⚙️
**Путь:** `/настройки_из_файла_env.txt`

Полный список всех переменных окружения с комментариями:
- Настройки VAD (`VAD_SILENCE_TIMEOUT`, `VAD_MIN_RECORDING_TIME`)
- Настройки Barge-in (`BARGE_IN_GUARD_MS`, `BARGE_IN_ENABLED`)
- Настройки Soft Window (`USE_SOFT_WINDOW_INPUT`, `CHUNK_STREAMING_SECONDS`)
- Настройки TTS (`TTS_PARALLEL_WORKERS`, `AUDIO_BUFFER_SIZE`)

**Критичные параметры для экспериментов:**
- `VAD_SILENCE_TIMEOUT=1.5`
- `BARGE_IN_GUARD_MS=800`
- `CHUNK_STREAMING_SECONDS=5`
- `TTS_PARALLEL_WORKERS=3`

---

## 🏗️ Структура проекта (краткая)

```
asterisk-vox-bot/
├── app/
│   ├── backend/
│   │   ├── asterisk/
│   │   │   └── stasis_handler_optimized.py  ← Главный обработчик Asterisk событий
│   │   ├── services/
│   │   │   ├── simple_vad_service.py         ← VAD логика (проблема #2)
│   │   │   ├── parallel_tts.py               ← TTS с очередями (проблема #1)
│   │   │   ├── soft_window_processor.py      ← Soft Window для записи
│   │   │   └── asterisk_stream_handler.py    ← Прямая работа с Asterisk
│   │   └── core/
│   │       └── config_base_settings.py       ← Pydantic BaseSettings конфиг
│   └── frontend/
│       └── admin_panel/                       ← Веб-интерфейс (не критично)
└── .env                                       ← Переменные окружения
```

**Ключевые файлы для изменений:**
1. `services/simple_vad_service.py` - основная логика VAD
2. `services/parallel_tts.py` - управление TTS очередью
3. `asterisk/stasis_handler_optimized.py` - обработка событий `ChannelTalkingStarted/Finished`
4. `services/soft_window_processor.py` - динамическая запись речи

---

## 🎯 Стратегия решения: РАЗБИТЬ НА МЕЛКИЕ ЗАДАЧИ

⚠️ **КРИТИЧНО:** Не пытайтесь решить всё сразу! Разбейте работу на **небольшие независимые задачи**, чтобы не переполнять контекстное окно.

### Рекомендуемая структура задач:

#### **Phase 1: Анализ и диагностика** (1-2 задачи)
- [ ] **Task 1.1:** Изучить `BARGE_IN_PROBLEM_ANALYSIS.md` и логи, составить список конкретных симптомов
- [ ] **Task 1.2:** Проанализировать код VAD и TTS, найти места возникновения проблем

#### **Phase 2: Эксперимент с VAD** (2-3 задачи)
- [ ] **Task 2.1:** Реализовать улучшенную логику адаптивного VAD (Experiment 3)
  - Файл: `simple_vad_service.py`
  - Изменения: динамические таймауты на основе частоты `activity_updates`
- [ ] **Task 2.2:** Добавить механизм "grace period" для непрерывной речи
  - Если `recording_duration > 5s` и `time_since_activity < 1s` → увеличить timeout
- [ ] **Task 2.3:** Тестирование и логирование новой VAD логики

#### **Phase 3: Оптимизация TTS Queuing** (2-3 задачи)
- [ ] **Task 3.1:** Реализовать Buffered Streaming (Experiment 1)
  - Файл: `parallel_tts.py`
  - Добавить предварительную буферизацию первых чанков
- [ ] **Task 3.2:** Добавить детекцию underrun и автоматическую паузу
  - При `Buffer underrun` → пауза 100-200ms для накопления буфера
- [ ] **Task 3.3:** Оптимизация размера очереди (`TTS_PARALLEL_WORKERS`, `AUDIO_BUFFER_SIZE`)

#### **Phase 4: Интеграция и тестирование** (1-2 задачи)
- [ ] **Task 4.1:** Интеграция всех изменений, проверка совместимости
- [ ] **Task 4.2:** Создание `CHANGES_SUMMARY.md` с описанием всех изменений

### 🔄 Рекомендуемый порядок работы:

1. **Начните с Phase 1** - не кодите сразу, сначала изучите проблему
2. **Каждая задача = 1 коммит** с понятным сообщением
3. **После каждой задачи** - проверяйте, что бот запускается
4. **Если контекст переполняется** - коммит текущих изменений, новое контекстное окно

---

## 📝 Предложенные эксперименты (из анализа)

### **🔬 Experiment 1: Buffered Streaming** (приоритет: HIGH)
**Проблема:** Underrun из-за недостатка аудио в очереди

**Решение:**
- Генерировать первые 2-3 чанка TTS **до начала воспроизведения**
- Добавить буфер в 500-1000ms перед первым чанком
- Использовать больше `TTS_PARALLEL_WORKERS` (4-5 вместо 3)

**Файл:** `parallel_tts.py`

**Псевдокод:**
```python
# В методе _generate_tts_chunks()
async def _generate_tts_chunks(self, chunks):
    # Сгенерировать первые 2 чанка ДО начала playback
    initial_buffer = []
    for i in range(min(2, len(chunks))):
        audio = await self.tts_service.synthesize(chunks[i])
        initial_buffer.append(audio)
    
    # Начать playback только после буферизации
    await self._start_playback(initial_buffer[0])
    
    # Остальные чанки в фоне
    ...
```

---

### **🔬 Experiment 2: Priority Queue** (приоритет: MEDIUM)
**Проблема:** Barge-in не всегда останавливает TTS быстро

**Решение:**
- Использовать `asyncio.PriorityQueue` вместо обычной очереди
- При barge-in - немедленная очистка низкоприоритетных задач
- Высокий приоритет для первых чанков

**Файл:** `parallel_tts.py`

---

### **🔬 Experiment 3: Adaptive VAD с частотным анализом** (приоритет: HIGH)
**Проблема:** VAD обрывает запись при непрерывной речи

**Решение:**
- Отслеживать **частоту** обновлений `activity_updates`
- Если `avg_interval < 2.0s` и `recording_duration > 3.0s` → считать речь непрерывной
- Увеличить `adaptive_timeout` до 3.0s для непрерывной речи

**Файл:** `simple_vad_service.py`

**Уже частично реализовано в текущей версии!** Требуется улучшение:
```python
# В методе _monitor_silence()
if len(activity_updates) >= 2:
    intervals = [activity_updates[i] - activity_updates[i-1] 
                 for i in range(1, len(activity_updates))]
    avg_interval = sum(intervals) / len(intervals)
    
    # НОВОЕ: Более агрессивная логика
    if avg_interval < 2.0 and recording_duration > 3.0:
        adaptive_timeout = 3.5  # Увеличено с 3.0 до 3.5
        logger.debug(f"VAD: Непрерывная речь, timeout={adaptive_timeout}s")
```

---

### **🔬 Experiment 4: Hybrid Detection** (приоритет: LOW)
**Проблема:** Asterisk `TALK_DETECT` иногда не срабатывает на тихую речь

**Решение:**
- Комбинировать `ChannelTalkingStarted` с файловым VAD
- Если файл записи растет (>8KB каждые 500ms) → считать активностью
- Дублирование сигналов активности

**Файл:** `stasis_handler_optimized.py`

---

## 📋 Требования к документации

### 1. **Создать `CHANGES_SUMMARY.md`** в корне проекта
Структура:
```markdown
# Краткое описание изменений

## Проблемы, которые решены
1. Buffer underrun при TTS
2. Преждевременное прерывание VAD

## Эксперименты, которые реализованы
- [x] Experiment 1: Buffered Streaming
- [x] Experiment 3: Adaptive VAD
- [ ] Experiment 2: Priority Queue (не потребовался)
- [ ] Experiment 4: Hybrid Detection (не потребовался)

## Измененные файлы
1. `services/simple_vad_service.py` - улучшена адаптивная логика VAD
2. `services/parallel_tts.py` - добавлена буферизация первых чанков
3. `asterisk/stasis_handler_optimized.py` - уточнена логика обработки событий

## Ключевые изменения в коде
### simple_vad_service.py (строки 248-273)
- Увеличен adaptive_timeout до 3.5s для непрерывной речи
- Добавлена проверка: if recording_duration > 8.0s → timeout 4.0s

### parallel_tts.py (строки 180-210)
- Предварительная генерация первых 2 чанков
- Буфер 500ms перед началом playback
- Увеличен TTS_PARALLEL_WORKERS до 4

## Новые переменные окружения
Нет (используются существующие)

## Тестирование
- Тесты с длинными фразами (>10 секунд)
- Проверка underrun в логах
- Проверка срабатывания barge-in
```

### 2. **Комментарии в коде**
Добавляйте комментарии с префиксом `# ✅ CTO.NEW:` для новых изменений:
```python
# ✅ CTO.NEW: Предварительная буферизация первых чанков (Experiment 1)
initial_buffer = await self._prebuffer_chunks(chunks[:2])
```

### 3. **Коммиты**
Формат сообщений коммитов:
```
feat(vad): улучшена адаптивная логика для непрерывной речи

- Увеличен adaptive_timeout до 3.5s при avg_interval < 2.0s
- Добавлена проверка recording_duration > 8.0s → timeout 4.0s
- Улучшено логирование частоты activity_updates

Связано с: BARGE_IN_PROBLEM_ANALYSIS.md (Experiment 3)
```

---

## 🌿 Git workflow

### Создание ветки
```bash
git checkout -b feature/vad-underrun-fix
```

### Коммиты по задачам
```bash
# После завершения Task 2.1
git add services/simple_vad_service.py
git commit -m "feat(vad): реализован адаптивный VAD с частотным анализом (Task 2.1)"

# После завершения Task 3.1
git add services/parallel_tts.py
git commit -m "feat(tts): добавлена буферизация первых чанков (Task 3.1)"
```

### Финальный push
```bash
git push origin feature/vad-underrun-fix
```

**Не создавайте Pull Request!** Просто запушьте ветку, другой агент проанализирует изменения локально.

---

## 🧪 Тестирование

### Минимальный набор проверок:
1. **Бот запускается без ошибок**
   ```bash
   systemctl status metrotech-bot
   ```

2. **В логах НЕТ:**
   - `Buffer underrun detected` (или значительно меньше)
   - Преждевременных `VAD: Тишина обнаружена` при активной речи

3. **В логах ЕСТЬ:**
   - `VAD: Адаптивный режим` (для длинных фраз)
   - `TTS: Предварительная буферизация` (для первых чанков)

### Дополнительные проверки (если возможно):
- Позвонить на бота, произнести длинную фразу (>10 секунд)
- Проверить, что бот не прерывает запись в середине фразы
- Проверить, что barge-in работает (можно прервать бота)

---

## ⚠️ Важные ограничения

1. **НЕ меняйте структуру проекта** (не переименовывайте файлы/папки)
2. **НЕ удаляйте существующий функционал** (только дополнения/улучшения)
3. **НЕ меняйте `.env` файл** (работайте с существующими переменными)
4. **НЕ трогайте frontend** (фокус только на backend)
5. **НЕ создавайте новые сервисы** (улучшайте существующие)

---

## 🎯 Ожидаемый результат

### Минимальный успех:
- ✅ Underrun встречается <5% от общего количества воспроизведений
- ✅ VAD не прерывает записи длинных фраз (>10 секунд) при непрерывной речи
- ✅ Barge-in продолжает работать стабильно

### Идеальный результат:
- ✅ Нет underrun в логах
- ✅ VAD корректно обрабатывает фразы до 60 секунд
- ✅ Улучшена скорость первого ответа (Time-To-First-Audio)
- ✅ Документация всех изменений в `CHANGES_SUMMARY.md`

---

## 📞 Контакты и вопросы

Если возникают вопросы или нужны уточнения:
- Изучите `BARGE_IN_PROBLEM_ANALYSIS.md` - там очень детальное описание
- Логи в `Логи_бота.txt` - примеры конкретных проблем
- Все конфигурационные параметры в `настройки_из_файла_env.txt`

---

## 🚀 Быстрый старт

1. Клонировать репозиторий
2. Создать ветку `feature/vad-underrun-fix`
3. Прочитать `BARGE_IN_PROBLEM_ANALYSIS.md` (разделы: Проблемы, Эксперименты)
4. Прочитать `Логи_бота.txt` (найти все `Buffer underrun` и `VAD: Тишина`)
5. Начать с **Task 1.1** (анализ и диагностика)
6. Двигаться по задачам последовательно
7. После завершения - создать `CHANGES_SUMMARY.md`
8. Запушить ветку `feature/vad-underrun-fix`

**Удачи! 🎉**

---

## 📚 Дополнительные ресурсы

- Asterisk ARI Documentation: https://wiki.asterisk.org/wiki/display/AST/Asterisk+18+ARI
- `TALK_DETECT` function: используется для генерации `ChannelTalkingStarted/Finished` событий
- Yandex SpeechKit: используется для TTS/ASR (настройки в `.env`)

---

*Этот документ создан для передачи задачи от одного AI агента к другому с целью экономии токенов в исходном чате.*

*Версия: 1.0*  
*Дата: 11 ноября 2025*  
*Проект: asterisk_bot_metrotest*

